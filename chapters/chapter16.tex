\chapter{What changes}
\label{ch:what-changes}
\epigraph{At the quantum scale there are no cats; at scales appropriate for astrophysics there are no mountains.}{— James Ladyman \& Don Ross, \textit{Every Thing Must Go} (2007)}

\section{Introduction: the gauntlet}
\label{sec:16:intro-gauntlet}

A theory that survives every possible result explains nothing; if it can't lose, it can't win.

For most of this book, the argument has been constructive. I argued that strict definitions fail, that mechanisms do explanatory work, and that projectibility is indexed to purpose (Chapter~\ref{ch:projectibility}). Then the case studies did their part: countability, definiteness, lexical categories, gender, grammaticality, social stabilization.

Now comes the expensive part: a framework earns its keep only when it names the outcomes that would kill it and shows its receipts.

So this final chapter is short on purpose. It doesn't add another slogan. It states a gauntlet: what this view predicts, what would falsify it, and what work follows if we take those risks seriously. In the spirit of \textit{words that won't hold still}, realism is earned only by risking loss, so the instruction is practical from the start: build studies that can make the framework wrong.

One running case will keep this honest: English \mention{data}. It behaves count-like in some contexts and mass-like in others. That boundary behaviour is not a side issue. It's the test bench.

Three diagnostics make the case concrete from the start: agreement (\mention{the data are} vs \mention{the data is}), tight count packaging (\mention{a datum}, \mention{three data}), and loose packaging (\mention{many data} vs \mention{much data}). Of the three, agreement often tells us as much about analyst communities as about \mention{data} itself.

\section{No level privilege, no level immunity}
\label{sec:16:no-level-privilege}

The framework keeps one structural claim from start to finish: no level is privileged in advance. A category can be real at phonological, morphosyntactic, semantic, discourse, or social grain, provided it is maintained and projectible at that grain.

But \enquote{no level privilege} never meant \enquote{anything goes}. It denies both preassigned crowns and exemptions from evidence.

Every proposed category owes the same three debts. First, show a profile that actually clusters. Second, identify stabilizers that could maintain that clustering. Third, show forward projection~-- what learning one case buys you for the next. In compact form: clustering invites stabilizers, stabilizers underwrite projection, and projection is where clustering earns its strongest support.

That is the discipline behind the flexibility. Traditional labels don't get grandfathered in. New decompositions don't get rewarded just for being novel. The \term{meta-Occam} principle from Chapter~\ref{ch:kinds-without-essences} still applies: be parsimonious about mechanisms, not about labels.

This also means comparative concepts can stay useful without guaranteeing a language-particular category in every grammar. We can compare \term{adjective}-like behaviour across languages even where no dedicated adjective category is maintained inside a given language.
That is a null case, not a framework failure: comparative structure can be projectible even when a matching language-particular category is absent.

\section{Synthesis: What the book now commits to}
\label{sec:16:synthesis}

By this point, the book owes a ledger. The argument no longer hangs on one slogan or one favourite case. It hangs on a set of commitments that should travel across domains.

First, categories are not definitions and not conveniences. They are maintained profiles: clusters that hold because mechanisms keep rebuilding them under pressure. That claim was developed in Part~II and then forced through case studies rather than protected from them.

Second, projectibility is field-relative but not arbitrary. Different analytical purposes can justify different carvings of overlapping data, but each carving still owes mechanism and prediction. We don't get realism by declaring one universal label right. We get it by showing what a category lets us forecast, and why.

Third, boundary behaviour is part of the evidence, not a stain on it. If maintenance is real, edges should fray in patterned ways, not as free noise. If maintenance weakens, the framework predicts non-synchronous drift and lagged lock failure. If those signatures never appear, the framework's credibility should drop.

Table~\ref{tab:16:synthesis-ledger} makes this explicit. The rows are not summaries of chapters; they are commitments with observable consequences and stress signals. All rows are to be evaluated the same way Chapter~\ref{ch:social-stabilization} set up the test: conditioned models (\(P(\text{Form}\mid D,R,C)\)) against pooled baselines (\(P(\text{Form})\)). Together they specify where the view should succeed and where it should fail: maintained profiles, purpose-indexed projection, and informative edges.

\begin{table}[htbp]
\centering
\caption{Synthesis ledger: cross-case commitments before the gauntlet.}
\label{tab:16:synthesis-ledger}
\small
\begin{tabular}{@{}p{0.16\textwidth}p{0.27\textwidth}p{0.27\textwidth}p{0.24\textwidth}@{}}
\toprule
Domain & Mechanism commitment & Observable signature & Downgrade signal \\
\midrule
Countability (Ch.~\ref{ch:countability}) & Tight semantic--morphosyntactic coupling maintained by multiple stabilizers. & Structured boundary behaviour: core diagnostics resist while edge diagnostics drift. & Near-uniform drift across diagnostics and speakers with no stable conditioned structure across repeated datasets. \\
\addlinespace
Definiteness/deitality (Ch.~\ref{ch:definiteness-and-deitality}) & Two coupled clusters, not one: semantic identifiability and formal packaging. & Stable decouplings (form without semantics; semantics without form). & Durable lockstep where decoupling is predicted. \\
\addlinespace
Lexical categories (Ch.~\ref{ch:lexical-categories}) & Mechanism density differs by class; not every traditional label names one category. & Robust noun/verb projection, thinner adjective regimes, wastebasket failures. & Equal projection across classes after matched diagnostics and conditioning controls. \\
\addlinespace
Pro-form gender (Ch.~\ref{ch:proform-gender}) & Designatum-driven selection maintained across a distributed pro-form inventory. & Personhood-conditioned selection and chain-coherence effects with graded edges. & No reliable designatum conditioning once context is controlled. \\
\addlinespace
Grammaticality + social stabilization (Chs.~\ref{ch:grammaticality-itself}, \ref{ch:social-stabilization}) & Grammaticality is maintained coupling; public stability depends on social conditioning structure. & Detector misfires are patterned; conditioned models recover structure from pooled noise. & Conditioning yields no predictive gain over pooled baselines. \\
\bottomrule
\end{tabular}
\end{table}

The table sets the burden of proof for the rest of this chapter. If these commitments are right, they should cash out as risky predictions, and if they are wrong, the gauntlet should expose that quickly. This table does commitment bookkeeping; Table~\ref{tab:16:gauntlet} handles operational tests and stress-test criteria for the running case.

\section{The empirical commitments}
\label{sec:16:empirical-commitments}

The maintenance view is only worth anything if it yields risky expectations. Here are two.
Prediction 1 tests whether conditioning recovers structure from apparent noise, following the Chapter~\ref{ch:social-stabilization} protocol (\(P(\text{Form}\mid D,R,C)\) versus \(P(\text{Form})\)). Prediction 2 tests whether drift has ordered lag rather than synchronous blur. The two stress tests then ask the same question from both sides: can projection survive with no measurable maintenance architecture, or do mechanistic models fail to beat convenience baselines? Reading the chapter this way keeps one inferential lane from commitment to revision.

\subsection{Prediction 1: Fraying is asymmetric, not random}
\label{sec:16:prediction-fraying}

Margins fray, but they don't fray at random: once conditioning is ignored, structure can masquerade as noise, and once structure is ignored, noise can masquerade as structure.

When stabilizers uncouple, we shouldn't see all diagnostics collapse at once, and we shouldn't see all edge items wobble in the same direction. We should see patterned decay keyed to which mechanism weakened first.

Chapter~\ref{ch:countability} gave the template: when a noun's anchor weakens, it doesn't jump instantly from one clean box to another. Some cues destabilize first; others hold. Chapter~\ref{ch:definiteness-and-deitality} showed the same architecture with a different split: semantic and morphosyntactic behaviour can drift out of step in constrained ways. Chapter~\ref{ch:lexical-categories} showed the typological analogue: edges fray where support is thin.

The measurement signature is concrete. At population level, boundary regions should show graded distributions. At individual level, speakers should still show mostly sharp commitments, with variance concentrated \emph{between} speakers more than \emph{within} speakers. If fraying turns out to be uniform free noise across diagnostics and speakers, the maintenance account has overfit.

In the \mention{data} case, the framework predicts specific decouplings: tight frames should erode first as singulative anchoring weakens, while looser frames and agreement can remain stable longer. The key point is not \enquote{some variation}. The key point is structured non-synchrony.

\subsection{Prediction 2: Drift has an order (the sticky-lock effect)}
\label{sec:16:prediction-sticky-lock}

A second prediction concerns time. Drift should have an order.

The expected sequence is this: usage and semantic pressure move first; morphosyntactic locks lag; then those locks fail in bursts rather than smoothing out forever. The lock is \enquote{sticky}: it can persist after the pressure profile that built it has shifted.

This is a diachronic and cross-linguistic claim. It predicts staged decoupling, not instant category death and not indefinite linear blur. It also predicts homology without identity: unrelated languages under similar communicative and processing pressures should converge on similar local fixes, even when the material details differ.

The lag claim is architectural, not hierarchical. Semantic, morphosyntactic, and discourse representations can update at different rates without any one level being ontologically primary.

Scope matters here, and it has three tiers. Language-particular predictions target concrete diagnostics inside one grammar. Comparative predictions target recurring \emph{shapes} of drift across grammars. Constructional predictions target specific form--meaning pairings regardless of lexical label, such as competition between \mention{these data are} and \mention{this data is} as partially distinct constructions.

Again, \mention{data} is useful as a stress test. If anchor loss (\mention{datum} recession) yields staged erosion~-- tight before loose~-- we get sticky lag. If everything moves in lockstep with no lag and no local bursts, the prediction fails.

Methodologically, this means trajectories, not snapshots. The right tools are timeline corpora, age-graded acquisition data, perturbation designs, and multi-timescale simulations (Chapter~\ref{ch:projectibility}).
For \mention{data}, this yields a developmental ordering prediction: in cohorts where \mention{datum} is absent from child-directed input, loose packaging (\mention{many data}) should stabilize before tight singulative packaging (\mention{a datum}).

\section{Model-criticism conditions}
\label{sec:16:falsification}

These are not decorative thought experiments; they are model-criticism conditions.

\subsection{Stress test A: Mechanism-light projection}
\label{sec:16:stress-essentialist}

Stress test A asks whether strong projection can persist after broad mechanism controls. Find a robust, cross-linguistically stable grammatical category that projects strongly while showing no identifiable maintenance structure: no acquisition bias, no alignment pressure, no functional anchoring, no institutional reinforcement, no attractor-like distributional profile. If that pattern replicates, the maintenance account needs substantive revision.

\subsection{Stress test B: Baseline parity after noise controls}
\label{sec:16:stress-nominalist}

Stress test B asks whether mechanistic models keep an out-of-sample advantage over convenience baselines once noise is controlled. Show that usage variation is effectively a random walk once sampling noise is controlled: no reliable conditioning by frequency, no alignment effects, no stable register or community attractors, no basin-like recovery after perturbation. If categories don't predict beyond convenience labels, the account needs scope reduction or re-specification.

Neither stress test is a one-off anomaly check. Both require durable, cross-dataset shortfall in the mechanism~-- projection link. If that shortfall persists, revise the framework's scope and mechanism claims rather than protecting it with ad hoc rescue moves.
To limit post hoc rescue, preregister the primary proxy family for \enquote{maintenance structure} (at minimum: acquisition bias, alignment pressure, functional anchoring, institutional reinforcement, attractor-like distributions), then report transparently how conclusions change under justified alternative specifications.
Evaluate evidence cumulatively by domain using out-of-sample predictive performance, calibration, uncertainty intervals, and robustness to reasonable model choices. When major domains repeatedly show no mechanism-linked lift, downgrade strongly; when results are mixed, narrow scope and revise components; when lift is robust and replicated, raise confidence proportionally.

\subsection{Serious downgrading and revision}
\label{sec:16:serious-downgrade}

Full replacement is not the only informative outcome; serious downgrading and selective revision matter too.

Partial pressure is expected in any live research program: one mechanism claim can fail in one domain while others still carry predictive weight elsewhere. That is correction, not collapse.

Serious downgrading is stronger. If repeated studies show weak or inconsistent support for patterned fraying, no reliable lag structure in drift, and no stable predictive lift over convenience labels in major domains (\eg\ countability, definiteness, lexical categories), then the framework should be treated as provisional heuristic vocabulary rather than settled explanatory ontology. That is not \enquote{business as usual}. It is a substantive loss of status.

\section{Operationalizing the gauntlet}
\label{sec:16:operationalizing}

To keep the gauntlet empirical, each claim needs observables and predeclared comparison criteria.

For the running case \mention{data}, the core variables are straightforward: agreement choice, tight-frame acceptance (\mention{a datum}, low cardinals), and loose-frame acceptance (\mention{many} vs \mention{much}), conditioned by register, community, cohort, and constructional context.

\begin{table}[htbp]
\centering
\caption{Gauntlet claims, observables, and predeclared stress-test criteria.}
\label{tab:16:gauntlet}
\small
\begin{tabular}{@{}p{0.20\textwidth}p{0.36\textwidth}p{0.36\textwidth}@{}}
\toprule
Claim & Observable signature & Outcome that strongly downgrades maintenance \\
\midrule
Asymmetric fraying & Boundary diagnostics drift in structured, non-synchronous ways; variance partitions by speaker/context more than pure item noise. & Diagnostics move as unstructured free noise; no stable variance structure across replications. \\
\addlinespace
Sticky-lock drift & Diachronic lag between usage/semantic shift and morphosyntactic lock shift; local burst dynamics near boundary transitions. & No lag and no burst structure: all dimensions move in lockstep or diffuse linearly without staged decoupling. \\
\addlinespace
Essentialist stress test & Category projects strongly under perturbation despite no identifiable maintenance architecture. & Persistent mechanism-light projection remains after preregistered proxy controls and robustness checks. \\
\addlinespace
Nominalist stress test & Baseline random-walk or convenience-label models match mechanistic models on out-of-sample prediction and perturbation recovery. & Mechanistic models show no consistent out-of-sample advantage once uncertainty and calibration are accounted for across datasets. \\
\bottomrule
\end{tabular}
\end{table}

Operational note. For asymmetric fraying, fit hierarchical models that separate speaker, item, and context variance; the key test is stable variance structure, not residual free noise. For sticky-lock drift, estimate lag with changepoint or state-space methods and report uncertainty on lag and burst estimates. For the nominalist challenge, compare mechanistic models against random-walk and convenience-label baselines on held-out time slices and perturbation-recovery tasks, reporting both effect size and uncertainty.
A preregisterable template can make this concrete: declare before fitting the comparison metric (default held-out log-loss), the smallest practically relevant gain for the domain, and the calibration diagnostics; then report posterior or resampling uncertainty and sensitivity to plausible model alternatives.
Treat this template as a default for this book's case studies, not a universal constant: domains can justify stricter or looser practical-gain targets with power, measurement reliability, and decision relevance.
Use held-out log-loss reduction as the default gain metric, evaluated on out-of-construction splits (novel construction families or contexts, not random token shuffles). Any non-default metric should be preregistered with a smallest effect size of interest, a power target, and a reliability floor.
As Chapters~\ref{ch:projectibility} and \ref{ch:countability} stress, didactic ABMs are mechanism sketches, not alibis; status judgments should rest on replicated empirical datasets and multi-seed sensitivity summaries, not on one toy trajectory.
Within the modeling lane, seed distributions and mechanism-ablation controls should be treated as internal consistency checks, not as standalone evidence of category reality.

\section{Conclusion: the zipper under load}
\label{sec:16:zipper-at-scale}

Chapter~\ref{ch:the-category-zipper} gave an image; this chapter treats it as an instrument, so the metaphor has to pass calibration.

If you've ever overpacked a suitcase until the zipper protested, you already understand boundary behaviour.
Put the zipper under load. Ask what keeps the teeth meshed. Ask where slip begins. Ask which perturbation makes the slider skip. Then test those claims across timescales, populations, and methods, under load.

That is the shift. The question is no longer \enquote{what are categories, in themselves?} It is: what keeps this category together, for whom, under which pressures, and for how long?

In practical terms, this changes tomorrow's workflow. Instead of arguing first about labels, we should preregister stabilizer-linked predictions, measure boundary variance explicitly, treat mismatch patterns as evidence rather than embarrassment, and design studies that can make the framework wrong.

If the maintenance story keeps showing predictive lift and calibrated fit under that gauntlet, confidence in its realism rises. Where it doesn't, revise its scope and mechanisms, and replace components that repeatedly underperform.
At the scales where categories project, they are as real as anything else we measure.

Words that won't hold still can still hold together~-- until they don't.
